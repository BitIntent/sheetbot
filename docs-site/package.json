{
  "name": "sheetbot-manual-site",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "docusaurus start --host 0.0.0.0 --port 3001",
    "build": "docusaurus build",
    "publish:help": "node scripts/publish-help.mjs",
    "release:help": "npm run build && npm run publish:help",
    "serve": "docusaurus serve --host 0.0.0.0 --port 3001",
    "clear": "docusaurus clear"
  },
  "dependencies": {
    "@docusaurus/core": "^3.6.3",
    "@docusaurus/preset-classic": "^3.6.3"
  }
}
